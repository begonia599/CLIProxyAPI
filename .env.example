# =============================================================
#  CLIProxyAPI — 服务器环境变量配置
#  复制此文件为 .env 并填入实际值
# =============================================================

# 应用端口 (默认 8317)
CPA_PORT=8317

# 部署标记 (可选, 用于日志区分环境)
DEPLOY=production
